#!/usr/bin/env sh

mode=$1
if [ "$mode" = "raise" ]; then
    brightnessctl s 2%+
elif [ "$mode" = "lower" ]; then
    brightnessctl s 2%-
fi
brightness=$(brightnessctl g)
percent=$(( brightness * 100 / 24242 ))
notify-send -t 500 "Bri: $percent%"
